name: Deploy ssrumaintltd to DigitalOcean

on:
  push:
    branches:
      - main

env:
  DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
  DOCKER_IMAGE_NAME: ssrumaintltd-app
  DOCKER_IMAGE_TAG: ${{ secrets.DOCKER_HUB_USERNAME }}/ssrumaintltd-app:latest

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ env.DOCKER_IMAGE_TAG }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to DigitalOcean Droplet
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          script: |
            mkdir -p ~/ssrumaintltd
            cd ~/ssrumaintltd

            cat << 'EOF' > docker-compose.yml
            version: '2.4'
            services:
              web:
                image: ${{ env.DOCKER_IMAGE_TAG }}
                container_name: ssrumaintltd-app
                restart: unless-stopped
                mem_limit: 180m
                mem_reservation: 80m
                ports:
                  - "3001:3000"
                env_file:
                  - .env.local
                deploy:
                  resources:
                    limits:
                      memory: 180M
                    reservations:
                      memory: 80M
                logging:
                  driver: "json-file"
                  options:
                    max-size: "10m"
                    max-file: "3"
            EOF

            echo "${{ secrets.DOCKER_HUB_TOKEN }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin
            docker compose pull
            docker compose up -d
            docker image prune -f
